upstream fastcgi_backend {
    server php:9000;  # <-- talk to the php service, not 127.0.0.1
}

server {
    listen 80;
    server_name _;

    # Magento root
    set $MAGE_ROOT /var/www/html;

    # Use Magento's provided nginx rules
    include /var/www/html/nginx.conf.sample;
}
