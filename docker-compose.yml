version: '3.8'

services:
  mysql:
    image: mysql:8.0
    container_name: magento-mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
      MYSQL_DATABASE: ${MYSQL_DATABASE}
      MYSQL_USER: ${MYSQL_USER}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD}
    volumes:
      - db_data:/var/lib/mysql
    ports:
      - "3306:3306"

  elasticsearch:
    image: ${ELASTICSEARCH_IMAGE}
    container_name: magento-elasticsearch
    environment:
      - discovery.type=single-node
      - xpack.security.enabled=false
      # Uncomment if ES is unstable on your machine:
      # - ES_JAVA_OPTS=-Xms1g -Xmx1g
    ports:
      - "9200:9200"

  redis:
    image: redis:7.2
    container_name: magento-redis
    ports:
      - "6379:6379"

  php:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: magento-php
    working_dir: /var/www/html
    volumes:
      - ./src:/var/www/html
    depends_on:
      - mysql
      - redis
      - elasticsearch
    env_file:
      - .env
    environment:
      - COMPOSER_ALLOW_SUPERUSER=1
    # Start our init script which will provision Magento and then run php-fpm
    command: ["bash", "/usr/local/bin/startup.sh"]
    ports:
      - "9000:9000"

  nginx:
    image: ${NGINX_IMAGE}
    container_name: magento-nginx
    depends_on:
      - php
    ports:
      - "8081:80"
    volumes:
      - ./src:/var/www/html
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf

volumes:
  db_data:
